{
	"name": "poke-catch",
	"private": true,
	"scripts": {
		"install": "npm --prefix poke-back install && npm --prefix poke-front install",
		"build": "npx --prefix poke-front vite build",
		"start": "npm start --prefix poke-back"
	}
}
